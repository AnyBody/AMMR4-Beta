AnyFolder Left = {
  /// Reference to folder containing joint angles used for initial positions
  AnyFolder &JointPos = ..Mannequin.Posture.Left;
};

Interface = { AnyFolder Left = { AnyFolder RotVectorMeasures = {}; };
};

SelectedOutput = { AnyFolder Left = {}; };
